<?xml version="1.0" encoding="utf-8"?>
<tabs>

    <tab
        activeColor="@color/bottom_bar_color_active"
        icon="@drawable/selector_dashboard"
        id="@+id/tab_local_songs"
        inActiveColor="@color/bottom_bar_color_inactive"
        title="Local songs" />

    <tab
        activeColor="@color/bottom_bar_color_active"
        icon="@drawable/selector_wifi_transfer"
        id="@+id/tab_network"
        inActiveColor="@color/bottom_bar_color_inactive"
        title="Net Work" />

    <tab
        activeColor="@color/bottom_bar_color_active"
        icon="@drawable/selector_settings"
        id="@+id/tab_settings"
        inActiveColor="@color/bottom_bar_color_inactive"
        title="@string/settings" />

</tabs>